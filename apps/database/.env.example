# Database Configuration - Neon PostgreSQL
# OutbreakLens Project

# Direct connection (for long-running servers)
DATABASE_URL="postgresql://[username]:[password]@[endpoint].region.aws.neon.tech/[database]?channel_binding=require&sslmode=require"

# Pooled connection (recommended for serverless/edge functions)
DATABASE_URL_POOLED="postgresql://[username]:[password]@[endpoint]-pooler.region.aws.neon.tech/[database]?channel_binding=require&sslmode=require"

# Clerk Authentication
VITE_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here

# Instructions:
# 1. Copy this file to apps/web/ as .env.local
# 2. Replace the placeholders with your actual Neon connection strings
# 3. Get your connection string from: https://console.neon.tech
# 4. Never commit the .env.local file to version control (it's already in .gitignore)
